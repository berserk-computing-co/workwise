// React Native types — commented out, not used in web app
// import { type FieldValues } from 'react-hook-form';
// import { type OnboardingForm } from '@workwise/screens';
// import { isNullOrUndefined } from '@workwise/utilities';
//
// export const validOnboardingData = (data: FieldValues): data is OnboardingForm => {
//   const _data: OnboardingForm = data as OnboardingForm;
//   return !isNullOrUndefined(_data.firmEmail) &&
//     !isNullOrUndefined(_data.firmName) &&
//     !isNullOrUndefined(_data.adminEmail) &&
//     !isNullOrUndefined(_data.adminName);
// };
