{
  "name": "web",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/web",
  "projectType": "application",
  "tags": [
    "type:app",
    "scope:frontend"
  ],
  "targets": {
    "check-types": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npx tsc --noEmit --project tsconfig.typecheck.json",
        "cwd": "apps/web"
      }
    }
  }
}
